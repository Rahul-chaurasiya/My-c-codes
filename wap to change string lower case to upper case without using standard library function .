

#include<stdio.h>
#include<conio.h>
main()

{
  char a[20];
  int x,i=0,n;

  printf("enter string in lower case:");
  gets(a);

  printf("string in lower case %s",a);
 // puts(a);

while(a[i] != '\0')
{
  a[i]= a[i]-32;
  i++;
}
 
 printf("\nupper case string %s",a);

}

output :

enter string in lower case:rahul
string in lower case rahul
upper case string RAHUL
